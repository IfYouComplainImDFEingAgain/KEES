{
  "name": "xenforo-enhancer-extension",
  "version": "4.0.0",
  "description": "Browser extension for xenforo sites",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "watch": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
