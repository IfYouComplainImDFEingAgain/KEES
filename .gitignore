# Ignore the ignore directory
ignore/

# Feature files the mobile build copies from extension/ at build time (see
# mobile/build.sh). Tracked in one place only, so they cannot drift.
mobile/src/features/user-muting.js
mobile/src/features/native-video-player.js
mobile/src/homepage-content.js
mobile/src/homepage-hide.css

# Build output
kees-mobile-*.zip
